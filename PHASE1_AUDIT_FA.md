# PHASE1_AUDIT_FA.md
## گزارش پایداری هسته (Phase 1) — HG Export ERP

این گزارش فقط دربردارنده‌ی کارهایی است که واقعاً روی کد اجرا و در مرورگر
(Chromium headless، به‌صورت زنده) تست شده‌اند. هیچ نتیجه‌ای بدون تست واقعی
اعلام «موفق» نشده است.

**مهم:** طبق دستور صریح، در این فاز به Dashboard، نمودارها، PDF، چاپ،
شجره گرافیکی، Theme، انیمیشن، ری‌دیزاین UI، لوگو، برندینگ و بهبودهای
Responsive دست زده نشد. منطق کسب‌وکار (`logic.js`: قوانین قبض/والد‌فرزند/
Split/تخصیص/موجودی) هم عیناً دست‌نخورده باقی ماند.

---

## ۱. مشکلات بحرانی (پیدا و رفع شد)

### ۱.۱ سیستم مودال — علت ریشه‌ای
**مشکل:** فرم‌هایی مثل «قبض جدید»، «تفکیک قبض»، «ثبت اظهار»، «ثبت شناور» و…
گاهی به‌جای باز شدن به‌صورت پنجره، در پایین صفحه رندر می‌شدند.

**علت واقعی (نه حدس — با اندازه‌گیری زنده تأیید شد):**
یک ری‌دیزاین قبلی، قوانین CSS مربوط به `.modal-overlay` و `.modal-box`
(که تمام قالب‌های JS از آن‌ها استفاده می‌کنند) را حذف کرده و به‌جایش یک
سیستم دیگر با کلاس‌های `.modal` / `.modal-backdrop` / `.open` / `.show`
اضافه کرده بود — کلاس‌هایی که هیچ‌کجای کد جاوااسکریپت واقعاً تولید
نمی‌شوند. نتیجه: عنصر مودال با `position: static` رندر می‌شد و در
مختصات y ≈ ۱۹۰۰ پیکسل (خارج از دید، پایین صفحه) می‌نشست — دقیقاً همان
علامتی که گزارش شده بود.

**راه‌حل:**
- CSS صحیح برای `.modal-overlay` (`position:fixed; inset:0`, پس‌زمینه
  تیره، وسط‌چین، اسکرول داخلی) و `.modal-box` اضافه شد.
- قوانین قدیمی و ناهماهنگ (`.modal`, `.modal-backdrop`, `.modal-header`,
  `.modal-footer`, `.modal.open/.show`) که هیچ‌جا استفاده نمی‌شدند حذف شدند.
- توابع `openModal`/`closeModal` (که از قبل مرکزی بودند، فقط فاقد رفتار
  استاندارد بودند) به یک **Modal Manager واحد** ارتقا یافتند:
  بستن با ESC، بستن با کلیک بیرون از پنجره، قفل اسکرول بدنه صفحه هنگام
  باز بودن مودال، فوکوس خودکار روی اولین فیلد، و مهم‌تر از همه — دقیقاً
  یک شنونده (listener) برای هرکدام که هنگام بسته‌شدن به‌درستی حذف می‌شود
  (بدون نشت حافظه یا شنونده تکراری).

**تست زنده:** ۹ نوع مودال مختلف (قبض جدید، ویرایش قبض، تفکیک، تخصیص،
تخصیص جدید، ویرایش تخصیص، ثبت اظهار، ویرایش اظهار، ثبت شناور، ویرایش
شناور، ثبت عملیات، ویرایش عملیات) به‌صورت جداگانه باز و با ESC بسته
شدند — همه با `position: fixed` درست کار کردند، صفر خطای کنسول.

**شدت قبل از رفع:** Critical (تجربه کاربری اصلی برنامه را مختل می‌کرد).

---

### ۱.۲ فرم «ثبت عملیات شناور» خطای جاوااسکریپت می‌داد
**مشکل:** هر بار که فرم «ثبت عملیات شناور» باز می‌شد، در کنسول این خطا
ثبت می‌شد:
```
TypeError: Cannot read properties of null (reading 'addEventListener')
    at openNewVesselModal (app.js:1446)
```

**علت:** این تابع نسخه‌ی قدیمیِ فرم (که فیلدهای `nvDeclaration`،
`nvWeight`، `nvReceiptSources` داشت) را فرض می‌کرد، در حالی‌که قالب فعلی
فرم (طبق ساده‌سازی قبلی به شناور/تاریخ ورود/تاریخ خروج/وضعیت) این فیلدها
را اصلاً ندارد. این یک نمونه‌ی کلاسیک «کنترلر و قالب هماهنگ نیستند» بود.

**راه‌حل:** کد مرده (مربوط به فیلدهای غیرموجود) کامل حذف شد.

**شدت قبل از رفع:** High (خطای مکرر در کنسول؛ اگرچه ظاهراً مودال باز
می‌شد، اما وضعیت پایدار سیستم را زیر سؤال می‌برد).

---

### ۱.۳ گزارش عملیات شناورها — ستون «قبض‌های تخصیص‌یافته» همیشه خالی بود
**مشکل:** طبق درخواست صریح Step 11، این ستون باید از رابطه واقعی
Operation → Allocation → Receipt محاسبه شود، نه متن ذخیره‌شده.

**یافته واقعی:** قالب رندر (`reportsPage`) به فیلد `v.allocated_receipts_text`
ارجاع می‌داد، اما در کنترلر (`renderReports`) این فیلد **اصلاً محاسبه
نمی‌شد** — فقط `declarations_text` و `computed_weight` ساخته می‌شدند.
نتیجه: این ستون همیشه علامت «—» نشان می‌داد، مستقل از داده واقعی.

**راه‌حل:** محاسبه‌ی واقعی اضافه شد: برای هر عملیات، اظهارهای مرتبط
پیدا می‌شوند → تخصیص‌های معتبر آن اظهارها پیدا می‌شوند → شماره قبض‌های
غیرتکراری از آن تخصیص‌ها استخراج و نمایش داده می‌شوند.

**تست زنده (روی داده واقعی وارد‌شده از اکسل):**
| شناور | قبض‌های تخصیص‌یافته (بعد از رفع) |
|---|---|
| M/V MEHDI12 | 725، 727 |
| M/V BOUSHEHR | 721، 722، 723 |
| M/V IMAN10 | 710، 717 |
| M/V VIGOROUS | 648 |

قبل از رفع همه‌ی این سطرها «—» نشان می‌دادند.

**شدت قبل از رفع:** Critical (گزارش اصلی‌ترین ادعای Step 11 را برآورده
نمی‌کرد و داده نادرست/ناقص نشان می‌داد).

---

### ۱.۴ نشتی شناسه داخلی در صفحه جزئیات اظهار
**مشکل:** صفحه جزئیات اظهار یک ردیف با برچسب «شناسه داخلی» داشت که مقدار
`declaration_no` خام (مثلاً `DEC-00001`) را نشان می‌داد — دقیقاً همان
چیزی که Step 9 صریحاً منع کرده («Internal IDs must never be shown to
users»).

**راه‌حل:** این ردیف با «شماره کوتاژ» (شماره واقعی اظهار) جایگزین شد.

**بررسی کامل پروژه:** تمام ۲۰+ محل دیگری که `declaration_no` در کد
ظاهر می‌شود بررسی شدند — همه یا در سطح دیتابیس/منطق داخلی هستند (درست و
بی‌خطر) یا از قبل با فال‌بک `koutaj_no || declaration_no` درست نمایش
داده می‌شوند. این تنها مورد نقض بود.

**شدت قبل از رفع:** Medium (یک محل مشخص، نه سراسری).

---

## ۲. مشکلات متوسط (رفع شد)

- جدول‌های اصلی (قبض‌ها، اظهارها، تخصیص‌ها، فهرست شناورها، عملیات
  شناورها، موجودی) فاقد نمایش «تعداد سطر» بودند — طبق Step 10 اضافه شد
  (مثال: «تعداد سطر: ۱۹»).

---

## ۳. مشکلات UI/UX
موردی فراتر از سیستم مودال (که در بخش ۱.۱ رفع شد) در این فاز بررسی/تغییر
داده نشد، چون طبق دستور صریح، ری‌دیزاین UI و بهبود Responsive خارج از
محدوده Phase 1 اعلام شده بود.

## ۴. مشکلات معماری
معماری فعلی (IndexedDB + رندر مبتنی بر توابع خالص + روتر مبتنی بر hash)
برای این حجم داده و تعداد صفحات مناسب و پایدار است. تنها ریشه معماریِ
واقعاً مشکل‌ساز همان جدایی‌افتادن CSS از قالب‌های JS بود (بخش ۱.۱) که در
این فاز رفع شد. تغییر معماری بزرگ‌تری لازم دیده نشد و انجام نشد.

## ۵. مشکلات IndexedDB
در این نوبت هیچ تغییری در نسخه یا ساختار Object Storeها لازم نبود.
Schema فعلی برای Phase 1 پایدار است.

## ۶. مشکلات Inventory
منطق موجودی (`logic.js`) دست‌نخورده باقی ماند. هیچ باگ جدیدی در این
لایه در طول تست‌های این فاز مشاهده نشد (تست‌های ایجاد/تخصیص/تفکیک/حذف
که در نشست‌های قبلی انجام شده بودند، مجدداً تکرار نشدند مگر در حد لازم
برای تأیید عدم رگرسیون ناشی از تغییرات مودال).

## ۷. مشکلات CRUD
CRUD برای قبض، اظهار، تخصیص، شناور (Master) و عملیات شناور به‌صورت زنده
تست شد:
- ایجاد شناور جدید → بلافاصله در جدول ظاهر شد (بدون رفرش).
- ایجاد عملیات جدید برای همان شناور → بلافاصله در جدول عملیات ظاهر شد.
- تلاش برای حذف شناوری که عملیات وابسته دارد → به‌درستی مسدود شد.
- بعد از حذف عملیات وابسته، حذف شناور مجاز شد و انجام گرفت.
هیچ باگ CRUD جدیدی پیدا نشد؛ رفتار موجود از قبل صحیح بود.

## ۸. مشکلات Dependency
رفتار حذف وابسته (Dependency-safe delete) برای شناور/عملیات تست و تأیید
شد. رفتار مربوط به قبض/اظهار/تخصیص از نشست‌های قبلی از قبل تست‌شده بود
و در این فاز مجدداً بازنویسی نشد.

## ۹. اختلاف Excel و Web
در این فاز بررسی جدیدی روی این مورد انجام نشد (خارج از محدوده Step‌های
خواسته‌شده این نوبت).

## ۱۰. سناریوهای تستی که سیستم فعلی موفق/ناموفق بود

| سناریو | نتیجه |
|---|---|
| باز شدن صحیح ۹ نوع مودال مختلف | ✅ موفق |
| بستن مودال با ESC | ✅ موفق |
| بستن مودال با کلیک بیرون | ✅ موفق |
| کلیک داخل مودال آن را نمی‌بندد | ✅ موفق |
| تقویم شمسی همزمان با ESC مودال را نمی‌بندد به‌جای خودش | ✅ موفق (رفع شد) |
| ایجاد شناور بدون رفرش در UI | ✅ موفق |
| ایجاد عملیات بدون رفرش در UI | ✅ موفق |
| حذف شناور دارای وابستگی | ✅ به‌درستی مسدود شد |
| گزارش عملیات شناور — قبض‌های واقعی | ✅ موفق (رفع شد) |
| نبود خطای کنسول در کل مسیرهای تست‌شده | ✅ صفر خطا (به‌جز ۴۰۳ فونت گوگل، نامرتبط) |
| تست چند تب هم‌زمان (race condition) | ⛔ NOT TESTED |
| تست روی مرورگر واقعی ویندوز ۷ | ⛔ NOT TESTED (فقط Chromium headless روی لینوکس) |
| Master Data (کالاها/انبارها/گمرک‌ها) CRUD | ⛔ NOT TESTED (خارج از محدوده این فاز؛ چنین CRUD‌ای هنوز پیاده‌سازی نشده) |

## ۱۱. ریسک‌های باقی‌مانده
- **فونت آفلاین:** `style.css` هنوز یک `@import` مستقیم به Google Fonts
  دارد (از فازهای قبل، خارج از محدوده این نوبت). روی سیستم بدون اینترنت
  بی‌صدا fail می‌شود (فونت جایگزین می‌آید)، اما همچنان نقض قانون آفلاین
  کامل است.
- **جدول‌های مرکز گزارش‌ها:** ستون «تعداد سطر» عمداً به این جدول‌ها اضافه
  نشد — تصمیمی احتیاطی برای دوری از قلمرو Phase 2 (چون این جدول‌ها بخشی
  از صفحه گزارش‌ها هستند). اگر مدنظر توست، در یک نوبت جدا قابل افزودن است.
  همچنین جدول «آخرین عملیات» در Dashboard دست نخورد چون خود Dashboard
  صراحتاً «DO NOT TOUCH» اعلام شده بود.
- **Race condition چند تب:** به‌صورت واقعی تست نشد (نیاز به دو تب واقعی
  هم‌زمان دارد که در محیط تست فعلی شبیه‌سازی نشد).

## ۱۲. محدودیت‌های شناخته‌شده
- تست‌ها فقط در Chromium headless روی لینوکس اجرا شدند، نه روی ویندوز ۷
  واقعی.
- Master Data (کالاها/انبارها/گمرک‌ها/تعیین ماهیت/رویدادها) هنوز فاقد
  رابط CRUD اختصاصی هستند — این خارج از محدوده صریح این فاز بود.

---

### فایل‌های تغییر‌یافته در این فاز
- `css/style.css` — رفع تعارض کلاس‌های مودال.
- `js/app.js` — Modal Manager مرکزی، رفع باگ `openNewVesselModal`، رفع
  محاسبه گزارش شناور، رفع نشتی شناسه داخلی، افزودن «تعداد سطر».
- `js/jalali.js` — رفع تداخل ESC بین تقویم و مودال (`stopPropagation`).

فایل `js/logic.js` (منطق کسب‌وکار) در این فاز **هیچ تغییری نکرد**.

---

## پیوست: اصلاحات این نوبت (بعد از بحث و تأیید)

### قانون تخصیص قبض اظهاری
طبق تأیید صریح، قانون قبلی («قبض اظهاری اصلاً قابل تخصیص نیست») که در نسخه‌ی
قبلی به‌عنوان یک Regression شناسایی شده بود، با قانونی که برای «مانده» از
قبل وجود داشت یکسان شد: **قبض اظهاری هم فقط در صورتی مستقیم تخصیص می‌گیرد
که کل موجودی فعلی‌اش یک‌بار و به یک اظهار تخصیص داده شود.** بعد از آن
موجودی صفر می‌شود و تخصیص دوباره به‌طور طبیعی مسدود می‌شود.

**باگ کناری پیدا و رفع شد:** دکمه «تخصیص» برای هر قبض اظهاری (چه تخصیص‌خورده
چه نخورده) به‌طور کامل و همیشگی غیرفعال بود — یک شرط قدیمی از قانون قبلی
که با تغییر بالا هماهنگ نشده بود. حذف شد؛ حالا فقط وقتی قبض واقعاً بسته
شده دکمه غیرفعال می‌شود.

### قفل ویرایش/حذف برای قبض تقسیم‌شده یا تخصیص‌خورده
`validateEditReceipt` به‌طور کامل بازنویسی شد: هر قبضی که (۱) فرزند دارد،
(۲) خودش حاصل یک تقسیم است، یا (۳) تخصیص فعال دارد — **کاملاً** قفل
می‌شود (نه فقط محدودیت وزنی). تابع جدید `canEditReceipt` هم اضافه شد تا
همین بررسی، قبل از باز شدن مودال ویرایش انجام شود و در صورت مسدود بودن،
بلافاصله با کلیک روی دکمه یک پیام فارسی واضح (Toast) نشان داده شود —
بدون باز شدن مودال — دقیقاً هم‌الگو با رفتار موجود دکمه حذف.

**تست‌های اجراشده (زنده):**
- قبض تازه بدون فرزند/تخصیص → ویرایش عادی کار می‌کند. ✅
- بعد از Split شدن، تلاش برای ویرایش والد → مودال باز نمی‌شود، Toast
  «چون تقسیم شده است» نمایش داده می‌شود. ✅
- فرزند تازه‌ساز (بدون تخصیص) → مودال باز نمی‌شود، Toast «چون حاصل یک
  تقسیم است». ✅
- قبض اظهاری تازه: دکمه تخصیص فعال، وزن به‌طور خودکار و قفل‌شده با کل
  موجودی پر می‌شود، تخصیص کامل موفق، موجودی صفر می‌شود. ✅
- تلاش حذف قبض اظهاری تخصیص‌خورده → مسدود با پیام واضح. ✅
- داده واقعی وارد‌شده از اکسل (۱۹ قبض) دست‌نخورده و سالم باقی ماند. ✅

---

## پیوست دوم: تثبیت نهایی Phase 1 (این نوبت)

### فایل‌های تغییر‌یافته
`js/app.js`, `js/logic.js` (بدون تغییر در این نوبت، فقط بررسی شد)، `css/style.css`

### ۱. تخصیص قبض اظهاری
دکمه‌های «تقسیم» و «تخصیص» در فهرست قبض‌ها و صفحه جزئیات، صفت `disabled`
را کامل از دست دادند. الان همیشه قابل‌کلیک هستند؛ بررسی مجاز/غیرمجاز
بودن عملیات به لحظه‌ی کلیک منتقل شد و در صورت مسدود بودن، بلافاصله یک
Toast فارسی واضح نشان داده می‌شود (بدون باز شدن مودال بی‌فایده).

### ۲. اعتبارسنجی اظهار تکمیل‌شده
هر دو مسیر ثبت تخصیص (از صفحه جزئیات قبض، و از دفتر تخصیص‌ها) حالا قبل
از ذخیره بررسی می‌کنند که آیا اظهار مقصد `status === "تکمیل شده"` است؛
در این صورت پیام دقیق زیر نمایش داده و ذخیره متوقف می‌شود:

> «این اظهار قبلاً تکمیل شده و امکان تخصیص جدید ندارد.»

**باگ واقعی پیدا و رفع شد:** در مسیر «دفتر تخصیص‌ها»، خطاهای اضافه (مثل
اظهار ابطال‌شده) به آرایه‌ی `v.errors` اضافه می‌شدند اما `v.valid` هرگز
دوباره محاسبه نمی‌شد — یعنی این خطاها در عمل **هیچ‌وقت جلوی ذخیره را
نمی‌گرفتند**، فقط متن خطا (که هم دیده نمی‌شد) اضافه می‌شد. این باگ رفع شد.

### ۳/۴. حذف/ویرایش وابسته
- ویرایش اظهار «تکمیل‌شده» حالا کاملاً مسدود است (هم پیش از باز شدن
  مودال با Toast، هم در لحظه‌ی ذخیره به‌عنوان لایه‌ی ایمنی دوم).
- ویرایش/حذف قبض، حذف اظهار/عملیات/شناور وابسته: طبق تست‌های Phase 1
  قبلی از قبل درست کار می‌کردند؛ در این نوبت مجدداً با داده واقعی
  بازتأیید شدند.
- **تصمیم آگاهانه:** برای «عملیات شناور»، قفل کامل ویرایش پیاده‌سازی
  نشد. چون فیلد شناور از قبل غیرقابل‌تغییر است و ویرایش وضعیت/تاریخ خروج
  پس از مرتبط‌شدن اظهار، بخش طبیعی و ضروری گردش‌کار است (وگرنه هرگز
  نمی‌شد یک عملیات را «تکمیل‌شده» علامت زد). اگر نظر دیگری داری اطلاع بده.

### ۵. دکمه‌ها
تمام دکمه‌های عملیاتی (تقسیم، تخصیص، ویرایش، حذف) در همه‌ی صفحات حالا
همیشه فعال هستند؛ منطق مسدودسازی فقط در لحظه‌ی کلیک اجرا و با Toast/پیام
داخل مودال گزارش می‌شود.

### ۶. چیدمان مودال — علت ریشه‌ای واقعی پیدا شد
با بررسی سیستماتیک تمام کلاس‌های CSS استفاده‌شده در قالب‌های JS در برابر
فایل style.css، مشخص شد **چهار کلاس حیاتی اصلاً تعریف نشده بودند**:
`.form-grid` (استفاده‌شده در تمام فرم‌های مودال)، `.error-list` (جعبه
خطای داخل مودال)، `.modal-actions` (ردیف دکمه‌های ذخیره/انصراف)، و
`.split-row`/`.split-summary` (فرم تفکیک).

بدون `.form-grid`، هر فیلد به‌صورت پیش‌فرض مرورگر (عرض کوچک ثابت، بدون
Grid) رندر می‌شد — دقیقاً همان چیزی که «فضای خالی بزرگ سمت چپ» و «عرض
نامساوی ورودی‌ها» را توضیح می‌دهد. هر ۴ کلاس با Grid/Flex مناسب،
هم‌عرض‌سازی ورودی‌ها، ترازبندی برچسب‌ها، و پشتیبانی از `.field-full`
اضافه شدند.

**تأیید شده به‌صورت برنامه‌ای (نه فقط بصری):** بعد از رفع، `getComputedStyle`
نشان داد `display:grid` با دو ستون دقیقاً هم‌عرض (۳۲۷px و ۳۲۷px) روی
مودال «ثبت قبض جدید» اعمال می‌شود؛ فیلد یادداشت (`field-full`) با موفقیت
کل عرض (۶۷۴px) را می‌گیرد.

**این می‌تواند توضیح‌دهنده‌ی گزارش‌های قبلی «پیام خطای ویرایش/حذف دیده
نمی‌شود» هم باشد** — چون `.error-list` هیچ استایلی نداشت، پیام خطا در
برخی فرم‌ها ممکن بود به‌صورت متن ساده و کم‌رنگ ظاهر شود و به‌سادگی از چشم
دور بماند، نه اینکه واقعاً تولید نمی‌شد.

### تست‌های اجراشده (زنده، روی پروفایل کاملاً تازه)
- بوت پاک، ۸ صفحه اصلی، صفر خطای کنسول (به‌جز ۴۰۳ فونت گوگل نامرتبط).
- دکمه‌های تخصیص/تقسیم روی قبض بسته/اظهاری: بدون `disabled`، کلیک →
  Toast دقیق، بدون باز شدن مودال. ✅
- تخصیص به اظهار «تکمیل‌شده» از هر دو مسیر ورودی: مسدود با پیام دقیق. ✅
- ویرایش اظهار «تکمیل‌شده»: مسدود، هم پیش از باز شدن مودال هم در ذخیره. ✅
- Grid مودال «ثبت قبض جدید»: `display:grid`، دو ستون ۳۲۷px مساوی،
  تأیید شده با تست برنامه‌ای مستقیم روی DOM. ✅
- داده واقعی اکسل (۱۹ قبض) دست‌نخورده باقی ماند. ✅

### باقی‌مانده / نیازمند تأیید تو
- ویرایش «عملیات شناور» عمداً قفل کامل نشد (توضیح در بالا) — منتظر نظرت هستم.
- سناریوی چیدمان مودال روی سیستم واقعی خودت (اسکرین‌شات‌های قبلی) هنوز
  به‌طور مستقل روی محیط خودت تأیید نشده؛ لطفاً بعد از این نسخه دوباره تست کن.

---

## پیوست سوم: باگ بحرانی Toast — پیدا و رفع شد

### مشکل
گزارش شد: «هیچ پیام هشدار یا خطایی موقع حذف و ویرایش نمیاد».

### علت ریشه‌ای واقعی
دقیقاً همان الگوی باگ‌های قبلی (`.form-grid`, `.error-list`): کلاس `.toast`
که برای **تمام** پیام‌های موفقیت/خطای برنامه استفاده می‌شود (حذف، ویرایش،
تخصیص، Split، همه چیز) فقط `border-radius` و `box-shadow` داشت — بدون
`position`, `background`, `color`, `padding`, `z-index`. عنصر `#toastRoot`
هم هیچ CSS نداشت. یعنی خودِ کد جاوااسکریپت پیام را می‌ساخت و درج می‌کرد
(تأیید شد با تست مستقیم DOM)، ولی از نظر بصری این پیام هیچ رنگ پس‌زمینه،
موقعیت ثابت، یا برجستگی نداشت و عملاً روی صفحه دیده نمی‌شد یا در جریان
عادی صفحه گم می‌شد.

### رفع
`#toastRoot` به `position:fixed` (پایین-چپ صفحه) با `z-index:5000` (بالاتر
از مودال‌ها) تبدیل شد. `.toast` رنگ پس‌زمینه سرمه‌ای (حالت عادی)، قرمز
(`.err`)، و سبز (`.ok`) با متن سفید و padding مناسب گرفت.

### تست زنده (تأیید شده، نه فرضی)
حذف قبض ۶۴۱ (که فرزند دارد) را امتحان کردم:
- کلاس واقعی عنصر: `toast err`
- رنگ پس‌زمینه محاسبه‌شده: `rgb(138, 31, 31)` (قرمز تیره) ✅
- موقعیت داخل Viewport: `x:20, y:609` (قابل مشاهده) ✅
- متن: «قبض والد دارای فرزند است و تا حذف کل شجره قابل حذف نیست...» ✅

این باگ می‌تواند توضیح‌دهنده‌ی تمام گزارش‌های قبلی «پیام خطا نمیاد» باشد —
نه فقط برای حذف/ویرایش، بلکه هر عملیاتی در کل برنامه که از Toast استفاده
می‌کند (که اکثر عملیات‌ها همین‌طورند).

### یک مورد کوچک جانبی
کلاس `.num` (استفاده‌شده در دو جدول گزارش برای اعداد) هم بدون CSS بود؛
یک ترازبندی ساده اضافه شد (تأثیر بصری جزئی، نه یک باگ عملکردی).
