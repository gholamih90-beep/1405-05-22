# به‌روزرسانی: تاریخ شمسی خودکار + شماره ردیف جدول‌ها

این مرحله فقط UI/نمایش را تغییر داده؛ به هیچ‌کدام از فایل‌های `logic.js`
(منطق قبض، والد/فرزند، موجودی، Split، تخصیص) دست زده نشده.

## اضافه شد
- `js/jalali.js`: تبدیل میلادی↔شمسی آفلاین (بدون کتابخانه/CDN) + تقویم
  پاپ‌آپ. الگوریتم با ۷۵۶ تاریخ round-trip و چند تاریخ مرجع معروف تست شد.
- همه‌ی فیلدهای تاریخ در کل برنامه (ثبت/ویرایش قبض، اظهار، تخصیص، عملیات
  شناور) به‌صورت readonly + انتخاب از تقویم شمسی هستند؛ تایپ دستی ممکن نیست.
- شماره ردیف (ستون «ردیف») به تمام جدول‌های اصلی اضافه شد: قبض‌ها، اظهارها،
  تخصیص‌ها، شناورها، عملیات شناورها، موجودی، و تمام جدول‌های داخل مرکز گزارش‌ها.
- باگ برچسب رفع شد: صفحه «دفتر تخصیص‌ها» ستون اظهار را با شناسه داخلی
  (DEC-xxx) نشان می‌داد؛ اکنون شماره واقعی اظهار (کوتاژ) نمایش داده می‌شود.

## تست انجام‌شده (واقعی، در مرورگر headless)
- بوت پروژه، بدون خطای کنسول.
- تمام صفحات اصلی (قبض‌ها/اظهارها/تخصیص‌ها/شناورها/موجودی/گزارش‌ها) با
  ستون ردیف صحیح رندر شدند.
- انتخاب تاریخ از تقویم شمسی و ذخیره‌ی رکورد جدید با موفقیت تست شد.

## تغییرنیافته (طبق درخواست صریح)
- `js/logic.js` و تمام قوانین کسب‌وکار قبض/والد/فرزند/موجودی/Split/تخصیص.
