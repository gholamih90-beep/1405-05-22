# یادداشت رفع اشکال راه‌اندازی (Phase 1)

## علت ریشه‌ای
برنامه هرگز واقعاً روی IndexedDB گیر نکرده بود. مشکل یک `ReferenceError`
هنگام اجرای همان ابتدای اسکریپت `js/app.js` بود:

- شیء بازگشتی ماژول `HGRender` (نزدیک انتهای بخش render.js ادغام‌شده در
  همین فایل) دو نام را با شکل کوتاه (shorthand) صادر می‌کرد:
  `declarationDetail` و `allocationsPage`.
- اما تابعی با این دو نام اصلاً در فایل تعریف نشده بود (در طول ادغام/ویرایش
  قبلی به‌طور کامل حذف شده بودند، هرچند در `app.js` هنوز فراخوانی
  می‌شدند: `HGRender.declarationDetail(...)` و `HGRender.allocationsPage(...)`).
- در نتیجه، همان لحظه‌ی اجرای `return { ..., declarationDetail, ... }`
  یک `ReferenceError: declarationDetail is not defined` رخ می‌داد —
  این خطا **همزمان با بارگذاری اسکریپت** پرتاب می‌شود، یعنی پیش از آنکه
  تابع `boot()` در پایین فایل اصلاً اجرا شود. به همین دلیل نه پیام خطا
  نمایش داده می‌شد و نه `dbStatus` هرگز از حالت «در حال اتصال...» خارج
  می‌گشت — چون خودِ اسکریپت اصلاً کامل بارگذاری/اجرا نمی‌شد.
- یک اشکال مشابه سوم هم کشف شد: `newAllocationModal` تعریف شده بود ولی
  در شیء بازگشتی صادر نشده بود؛ پس `HGRender.newAllocationModal` تا قبل
  از رفع اشکال، `undefined` بود.

تشخیص با اجرای واقعی صفحه در یک مرورگر Chromium headless (نه با حدس)
انجام شد؛ خطای کنسول دقیقاً همین بود: `declarationDetail is not defined`.

## فایل‌ها و بخش‌های دقیق مسئول
- `js/app.js` — ماژول `HGRender` (خط‌های نزدیک ۵۹۲ تا ۶۰۰ در نسخه قبلی):
  دو تابع گمشده و یک export گمشده.

## رفع انجام‌شده
1. دو تابع `declarationDetail(decl, allocations)` و
   `allocationsPage(allocations)` بازنویسی و به ماژول `HGRender` اضافه شد
   (با همان قراردادهای داده‌ی فعلی پروژه: `koutaj_no`, `receipt_no`,
   `declaration_no`, لینک‌های `data-goto-receipt` / `data-goto-declaration`).
2. `newAllocationModal` به شیء صادراتی `HGRender` اضافه شد.
3. علاوه بر رفع اشکال اصلی، طبق درخواست، لایه‌ی مقاوم‌سازی راه‌اندازی هم
   اضافه شد تا این دسته از خطاها دیگر هرگز به‌صورت بی‌صدا برنامه را برای
   همیشه معلق نگه ندارد:
   - `js/db.js`: افزودن `onblocked` روی `indexedDB.open` (با پیام فارسی
     روشن دربارهٔ تب‌های باز دیگر) + پیام خطای غنی‌شده شامل نام عملیات،
     نام Store، نام و نسخهٔ پایگاه داده برای تمام عملیات (`getAll`, `get`,
     `put`, `delete`, `clear`, `byIndex`, `open`).
   - `js/app.js`: افزودن Watchdog Timeout ده‌ثانیه‌ای (`withTimeout`) دور
     مراحل `HGDB.open()` و `HGDB.seedIfEmpty()`؛ در صورت timeout یا هر
     خطای دیگر، به‌جای ماندن روی «در حال اتصال...»، یک پنل تشخیصی واضح
     (پیام خطا + نام/نسخهٔ پایگاه داده + زمان خطا) به همراه دکمهٔ
     «تلاش مجدد» نمایش داده می‌شود، و خطا با `console.error` هم ثبت می‌شود.

## نسخه پایگاه داده پس از رفع اشکال
`DB_NAME = hg_export_erp`, `DB_VERSION = 2` (بدون تغییر — مشکل مربوط به
نسخه یا ساختار Store نبود).

## Object Storeهای موجود
receipts, declarations, allocations, vessels, projects, operations,
settings, items, warehouses, customs, events, nature_determinations

## نتایج تست (اجرای واقعی، نه فرضی)
تست‌ها با Playwright + Chromium headless واقعی روی همین پروژه اجرا شدند:

| تست | نتیجه |
|---|---|
| راه‌اندازی تمیز (پروفایل خالی مرورگر) | ✅ موفق — دیگر روی «در حال اتصال...» گیر نمی‌کند |
| تعداد قبض‌های بارگذاری‌شده | ✅ ۱۹ (مطابق اکسل) |
| تعداد اظهارها | ✅ ۸ (مطابق اکسل) |
| تعداد تخصیص‌ها | ✅ ۱۰ (مطابق اکسل) |
| باز کردن قبض ۶۴۱ (دارای فرزند) | ✅ صفحه جزئیات به‌درستی رندر شد |
| باز کردن شجره قبض ۶۴۱ | ✅ رندر شد |
| باز کردن فهرست اظهارها → مشاهده اظهار DEC-00001 | ✅ صفحه جزئیات اظهار رندر شد |
| باز کردن دفتر تخصیص‌ها | ✅ رندر شد |
| راه‌اندازی مجدد با پروفایل موجود (بدون حذف IndexedDB) | ✅ داده‌ها حفظ شدند، بدون رکورد تکراری (هنوز ۱۹ قبض) |
| Hard refresh در همان تب | ✅ بدون گیر کردن، داده‌ها حفظ شدند |
| خطای کنسول جاوااسکریپت در تمام تست‌های بالا | ✅ صفر خطا |

تست‌های زیر در این نوبت اجرا **نشدند** (NOT TESTED) — نیاز به شبیه‌سازی
واقعی چند تب هم‌زمان یا قطع شبکه دارند که در محیط تست فعلی امکان‌پذیر
نبود؛ کد دفاعی برای آن‌ها اضافه شده اما رفتار واقعی مرورگر تأیید نشده:
- سناریوی واقعی `onblocked` با دو تب واقعی هم‌زمان باز
- رفتار روی خودِ ویندوز ۷ با مرورگر واقعی (تست فقط در Chromium headless
  روی لینوکس انجام شد)

## پاک‌سازی جانبی
فایل `js/render.js` که در ZIP قبلی وجود داشت اما در `index.html` هرگز
بارگذاری نمی‌شد (کد HGRender در همان `js/app.js` ادغام شده بود) حذف شد
تا از وجود دو نسخهٔ واگرا و گمراه‌کننده از همان کد جلوگیری شود.
