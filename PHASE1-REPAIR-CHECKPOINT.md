# HG ERP — Phase 1 Integrity Repair

این نسخه با تمرکز روی یکپارچگی داده، موجودی و وابستگی‌های قبض‌ها اصلاح شده است.

## قواعد اصلی نسخه

- قبض جدید فقط از نوع «اصلی» ایجاد می‌شود.
- قبض‌های «اظهاری» و «مانده» فقط از مسیر Split ایجاد می‌شوند.
- هر Split معتبر دقیقاً دو فرزند دارد: یک «اظهاری» و یک «مانده».
- اگر کل موجودی قرار است تخصیص داده شود، Split مجاز نیست و تخصیص مستقیم انجام می‌شود.
- قبض «مانده» فقط در صورت تخصیص کامل موجودی فعلی می‌تواند Allocation مستقیم بگیرد.
- قبض «اظهاری» نیز برای حفظ تراز Split باید به طور کامل تخصیص یابد.
- وزن قبض مانده در فرم Split خودکار محاسبه می‌شود.
- حذف یکی از دو فرزند Split به‌صورت مستقل مجاز نیست؛ درخواست حذف یکی، کل Split را هدف می‌گیرد.
- وجود هر وابستگی در یکی از دو فرزند، حذف کل Split را مسدود می‌کند.
- پس از حذف Split، وزن آزاد والد مجدداً از روی داده‌ها محاسبه می‌شود.
- موجودی انبار مقدار Derived است و به‌عنوان عدد دستی ذخیره نمی‌شود.
- قبض اظهاری در موجودی انبار دوباره شمارش نمی‌شود.
- ویرایش وزن یکی از فرزندان Split، وزن فرزند هم‌گروه را به‌صورت خودکار برای حفظ تراز تنظیم می‌کند؛ مشروط به اینکه تخصیص‌های فعال نقض نشوند.
- تغییرات حساس Split/Delete در یک IndexedDB transaction انجام می‌شوند.
- عملیات شناور چهار وضعیت دارد: «در انتظار ورود»، «در حال عملیات»، «تکمیل شده»، «لغو شده».
- وضعیت UI بعد از CRUD بدون Refresh دستی بازسازی می‌شود.

## آزمون منطقی انجام‌شده

- موجودی اولیه داده‌های Excel: `14,688.187` تن
- موجودی محاسبه‌شده موتور جدید: `14,688.187` تن
- تخصیص جزئی قبض مانده: رد می‌شود.
- تخصیص کامل قبض مانده: مجاز است.
- تخصیص جزئی قبض اظهاری: رد می‌شود.
- Split با دو فرزند و مجموع برابر موجودی: مجاز است.
- Split با مصرف کل موجودی: رد می‌شود و باید Allocation مستقیم انجام شود.
- حذف Split بدون وابستگی: مجاز.
- حذف Split دارای وابستگی: رد.
